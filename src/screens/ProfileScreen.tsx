import { View, Text, SafeAreaView } from "react-native";

const ProfileScreen = ({}) => {
	return (
		<SafeAreaView>
			<View>
				<Text>Profile Screen</Text>
			</View>
		</SafeAreaView>
	);
};

export default ProfileScreen;
