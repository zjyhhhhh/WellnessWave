import { View, Text, SafeAreaView } from "react-native";

const RecordScreen = ({}) => {
	return (
		<SafeAreaView>
			<View>
				<Text>Record Screen</Text>
			</View>
		</SafeAreaView>
	);
};

export default RecordScreen;
