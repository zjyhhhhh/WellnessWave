import { View, Text, SafeAreaView } from "react-native";

const ProfileScreen = ({}) => {
	return (
		<SafeAreaView>
			<View>
				<Text>Moment Screen</Text>
			</View>
		</SafeAreaView>
	);
};

export default ProfileScreen;
